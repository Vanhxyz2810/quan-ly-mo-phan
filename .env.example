# SQL Server SA password (min 8 chars, uppercase + lowercase + digit + special)
SA_PASSWORD=CemeteryIQ@2026!

# JWT signing key
JWT_KEY=CemeteryIQ-Super-Secret-Key-2026-Do-Not-Share!

# Frontend URL (for CORS)
CORS_ORIGIN=http://localhost:3000

# Backend API URL (used at build time for Next.js)
API_URL=http://localhost:5154
